<% provide(:title, 'All users') %>
<div id="pagewrap">
<h1>All users</h1>

<%= will_paginate %>
<div id="userlist">
 <ul class="users">
  <%= render @users %>
</ul>

</div>
<%= will_paginate %>

</div>